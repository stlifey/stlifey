diff -urN a/src/VBox/Frontends/VBoxBFE/Makefile.kmk b/src/VBox/Frontends/VBoxBFE/Makefile.kmk
--- a/src/VBox/Frontends/VBoxBFE/Makefile.kmk	2013-03-05 22:27:07.000000000 +0800
+++ b/src/VBox/Frontends/VBoxBFE/Makefile.kmk	2013-03-25 04:11:54.080853926 +0800
@@ -125,14 +125,6 @@
 VBoxBFE_CXXFLAGS.linux =  \
 	-fno-rtti -fno-exceptions -fshort-wchar -pthread
 
-VBoxBFE_CLEAN = $(VBoxBFE_0_OUTDIR)/Ico64x01.h
-VBoxBFE_SDLConsole.cpp_DEPS = $(VBoxBFE_0_OUTDIR)/Ico64x01.h
-
-# Convert the pnm-file to a byte array.
-$$(VBoxBFE_0_OUTDIR)/Ico64x01.h: $(PATH_ROOT)/src/VBox/Frontends/VBoxBFE/ico64x01.pnm $(VBOX_BIN2C) | $$(dir $$@)
-	$(call MSG_TOOL,bin2c,VBoxBFE,$<,$@)
-	$(QUIET)$(VBOX_BIN2C) Ico64x01 $< $@
-
 # Files we share with Main needs to be copied into the output dir.
 VBoxBFE_INTERMEDIATES  = $(addprefix $(VBoxBFE_0_OUTDIR)/, $(VBOXBFE_MAIN_HDRS))
 VBoxBFE_CLEAN         += $(addprefix $(VBoxBFE_0_OUTDIR)/, $(VBOXBFE_MAIN_HDRS) $(notdir $(VBOXBFE_MAIN_SRCS)))
diff -urN a/src/VBox/Frontends/VBoxBFE/SDLConsole.cpp b/src/VBox/Frontends/VBoxBFE/SDLConsole.cpp
--- a/src/VBox/Frontends/VBoxBFE/SDLConsole.cpp	2013-03-05 22:27:07.000000000 +0800
+++ b/src/VBox/Frontends/VBoxBFE/SDLConsole.cpp	2013-03-25 04:12:27.707852035 +0800
@@ -72,7 +72,6 @@
 
 #include "ConsoleImpl.h"
 #include "SDLConsole.h"
-#include "Ico64x01.h"
 
 /*******************************************************************************
 *   Internal Functions                                                         *
@@ -88,7 +87,6 @@
     /** Custom window manager cursor? */
     gpCustomWMcursor = NULL;
     mfInitialized    = false;
-    mWMIcon          = NULL;
 
     memset(gaModifiersState, 0, sizeof(gaModifiersState));
 
@@ -118,17 +116,6 @@
     }
 #endif
 
-    if (12320 == g_cbIco64x01)
-    {
-        mWMIcon = SDL_AllocSurface(SDL_SWSURFACE, 64, 64, 24, 0xff, 0xff00, 0xff0000, 0);
-        /** @todo make it as simple as possible. No PNM interpreter here... */
-        if (mWMIcon)
-        {
-            memcpy(mWMIcon->pixels, g_abIco64x01+32, g_cbIco64x01-32);
-            SDL_WM_SetIcon(mWMIcon, NULL);
-        }
-    }
-
     /*
      * Enable keyboard repeats
      */
@@ -155,12 +142,6 @@
      * Uninitialise "children" (so far only Mouse)
      */
     gMouse->uninit();
-
-    if (mWMIcon)
-    {
-        SDL_FreeSurface(mWMIcon);
-        mWMIcon = NULL;
-    }
 }
 
 CONEVENT SDLConsole::eventWait()
diff -urN a/src/VBox/Frontends/VBoxSDL/Framebuffer.cpp b/src/VBox/Frontends/VBoxSDL/Framebuffer.cpp
--- a/src/VBox/Frontends/VBoxSDL/Framebuffer.cpp	2013-03-05 22:27:09.000000000 +0800
+++ b/src/VBox/Frontends/VBoxSDL/Framebuffer.cpp	2013-03-25 04:10:22.260859088 +0800
@@ -40,7 +40,6 @@
 
 #include "VBoxSDL.h"
 #include "Framebuffer.h"
-#include "Ico64x01.h"
 
 #if defined(RT_OS_WINDOWS) || defined(RT_OS_LINUX)
 #include <SDL_syswm.h>           /* for SDL_GetWMInfo() */
@@ -67,7 +66,6 @@
 #endif /* VBOX_SECURELABEL */
 
 static bool gfSdlInitialized = false;           /**< if SDL was initialized */
-static SDL_Surface *gWMIcon = NULL;             /**< the application icon */
 static RTNATIVETHREAD gSdlNativeThread = NIL_RTNATIVETHREAD; /**< the SDL thread */
 
 //
@@ -220,17 +218,6 @@
                          RTEnvGet("SDL_VIDEODRIVER"));
     }
 
-    if (12320 == g_cbIco64x01)
-    {
-        gWMIcon = SDL_AllocSurface(SDL_SWSURFACE, 64, 64, 24, 0xff, 0xff00, 0xff0000, 0);
-        /** @todo make it as simple as possible. No PNM interpreter here... */
-        if (gWMIcon)
-        {
-            memcpy(gWMIcon->pixels, g_abIco64x01+32, g_cbIco64x01-32);
-            SDL_WM_SetIcon(gWMIcon, NULL);
-        }
-    }
-
     return true;
 }
 
@@ -245,11 +232,6 @@
     {
         AssertMsg(gSdlNativeThread == RTThreadNativeSelf(), ("Wrong thread! SDL is not threadsafe!\n"));
         SDL_QuitSubSystem(SDL_INIT_VIDEO);
-        if (gWMIcon)
-        {
-            SDL_FreeSurface(gWMIcon);
-            gWMIcon = NULL;
-        }
     }
 }
 
diff -urN a/src/VBox/Frontends/VBoxSDL/Makefile.kmk b/src/VBox/Frontends/VBoxSDL/Makefile.kmk
--- a/src/VBox/Frontends/VBoxSDL/Makefile.kmk	2013-03-05 22:27:09.000000000 +0800
+++ b/src/VBox/Frontends/VBoxSDL/Makefile.kmk	2013-03-25 04:11:27.875855399 +0800
@@ -94,15 +94,6 @@
 VBoxSDL_LDFLAGS.darwin = \
 	-framework Foundation -framework AppKit
 
-VBoxSDL_CLEAN = $(VBoxSDL_0_OUTDIR)/Ico64x01.h
-VBoxSDL_INTERMEDIATES = $(VBoxSDL_0_OUTDIR)/Ico64x01.h
-
-
-# Convert the pnm-file to a byte array.
-$$(VBoxSDL_0_OUTDIR)/Ico64x01.h: $(PATH_ROOT)/src/VBox/Frontends/VBoxSDL/ico64x01.pnm $(VBOX_BIN2C) | $$(dir $$@)
-	$(call MSG_TOOL,bin2c,VBoxSDL,$<,$@)
-	$(QUIET)$(VBOX_BIN2C) Ico64x01 $< $@
-
 # Icon include file.
 $$(VBoxSDL_0_OUTDIR)/VBoxSDL-icon.rc: $(VBOX_WINDOWS_ICON_FILE) $$(VBoxSDL_DEFPATH)/Makefile.kmk | $$(dir $$@)
 	$(RM) -f $@
